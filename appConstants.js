const BASE_URL = `http://localhost:8000/`;

// const DOMAIN = `https://frontend.k-kumar-01.vercel.app/`;
const DOMAIN = `http://localhost:3000`;

// others
const COOKIE_NAME = "token";

// urls of backend
const UPLOADS = `${BASE_URL}uploads`;
const SIGNUP = `${BASE_URL}api/user/signup`;
const LOGIN = `${BASE_URL}api/user/login`;
export { BASE_URL, DOMAIN, COOKIE_NAME, UPLOADS, SIGNUP, LOGIN };
